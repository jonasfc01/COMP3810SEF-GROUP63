<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= mode === 'create' ? 'Create' : 'Edit' %> Task - Task Manager</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Task Manager</h1>
            <div class="user-info">
                Welcome, <%= user.username %>!
                <a href="/tasks" class="btn btn-secondary">Back to Tasks</a>
                <form action="/logout" method="POST" style="display: inline;">
                    <button type="submit" class="btn-logout">Logout</button>
                </form>
            </div>
        </header>
        
        <nav class="sidebar">
            <ul>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/tasks">View All Tasks</a></li>
                <li><a href="/tasks/create" class="active">Create New Task</a></li>
                <% if (user.username === 'admin') { %>
                    <li><a href="/users">Manage Users</a></li>
                    <li><a href="/users/create">Create User</a></li>
                <% } %>
            </ul>
        </nav>
        
        <main class="content">
            <div class="form-container">
                <h2><%= mode === 'create' ? 'Create New Task' : 'Edit Task' %></h2>
                
                <% if (error) { %>
                    <div class="error"><%= error %></div>
                <% } %>

                <form method="POST" action="<%= mode === 'create' ? '/tasks/create' : '/tasks/edit/' + task._id %>">
                    <div class="form-group">
                        <label for="title">Title *</label>
                        <input type="text" id="title" name="title" value="<%= task ? task.title : '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea id="description" name="description" rows="4"><%= task ? (task.description || '') : '' %></textarea>
                    </div>
                    
                    <div class="form-group">
                        <label for="priority">Priority</label>
                        <select id="priority" name="priority">
                            <option value="low" <%= task && task.priority === 'low' ? 'selected' : '' %>>Low</option>
                            <option value="medium" <%= task && task.priority === 'medium' ? 'selected' : (!task ? 'selected' : '') %>>Medium</option>
                            <option value="high" <%= task && task.priority === 'high' ? 'selected' : '' %>>High</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="status">Status</label>
                        <select id="status" name="status">
                            <option value="pending" <%= task && task.status === 'pending' ? 'selected' : (!task ? 'selected' : '') %>>Pending</option>
                            <option value="in-progress" <%= task && task.status === 'in-progress' ? 'selected' : '' %>>In Progress</option>
                            <option value="completed" <%= task && task.status === 'completed' ? 'selected' : '' %>>Completed</option>
                        </select>
                    </div>
                    
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <%= mode === 'create' ? 'Create Task' : 'Update Task' %>
                        </button>
                        <a href="/tasks" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </main>
    </div>
</body>
</html>

