<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users - Task Manager</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Task Manager</h1>
            <div class="user-info">
                Welcome, <%= user.username %>!
                <form action="/logout" method="POST" style="display: inline;">
                    <button type="submit" class="btn-logout">Logout</button>
                </form>
            </div>
        </header>
        
        <nav class="sidebar">
            <ul>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/tasks">View All Tasks</a></li>
                <li><a href="/tasks/create">Create New Task</a></li>
                <li><a href="/users" class="active">Manage Users</a></li>
                <li><a href="/users/create">Create User</a></li>
            </ul>
        </nav>
        
        <main class="content">
            <div class="tasks-header">
                <h2>User Management</h2>
                <a href="/users/create" class="btn btn-primary">+ Create New User</a>
            </div>

            <% if (error) { %>
                <div class="error"><%= error %></div>
            <% } %>

            <% if (users && users.length > 0) { %>
                <div class="table-container">
                    <table class="data-table">
                        <thead>
                            <tr>
                                <th>Username</th>
                                <th>Email</th>
                                <th>Created At</th>
                                <th>Updated At</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(userData => { %>
                                <tr>
                                    <td><strong><%= userData.username %></strong></td>
                                    <td><%= userData.email || 'N/A' %></td>
                                    <td><%= new Date(userData.createdAt).toLocaleString() %></td>
                                    <td><%= new Date(userData.updatedAt).toLocaleString() %></td>
                                    <td>
                                        <div class="table-actions">
                                            <a href="/users/edit/<%= userData._id %>" class="btn btn-sm btn-edit">Edit</a>
                                            <form action="/users/delete/<%= userData._id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this user?');">
                                                <button type="submit" class="btn btn-sm btn-delete">Delete</button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <p>No users found. Create your first user to get started!</p>
                    <a href="/users/create" class="btn btn-primary">Create User</a>
                </div>
            <% } %>
        </main>
    </div>
</body>
</html>

