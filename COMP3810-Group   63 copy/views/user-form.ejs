<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= mode === 'create' ? 'Create' : 'Edit' %> User - Task Manager</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Task Manager</h1>
            <div class="user-info">
                Welcome, <%= user.username %>!
                <a href="/users" class="btn btn-secondary">Back to Users</a>
                <form action="/logout" method="POST" style="display: inline;">
                    <button type="submit" class="btn-logout">Logout</button>
                </form>
            </div>
        </header>
        
        <main class="content">
            <div class="form-container">
                <h2><%= mode === 'create' ? 'Create New User' : 'Edit User' %></h2>
                
                <% if (error) { %>
                    <div class="error"><%= error %></div>
                <% } %>

                <form method="POST" action="<%= mode === 'create' ? '/users/create' : '/users/edit/' + userData._id %>">
                    <div class="form-group">
                        <label for="username">Username *</label>
                        <input type="text" id="username" name="username" value="<%= userData ? userData.username : '' %>" required minlength="3" maxlength="30">
                    </div>
                    
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" value="<%= userData ? (userData.email || '') : '' %>">
                    </div>
                    
                    <div class="form-group">
                        <label for="password">Password <%= mode === 'edit' ? '(leave blank to keep current)' : '*' %></label>
                        <input type="password" id="password" name="password" <%= mode === 'create' ? 'required minlength="6"' : '' %>>
                        <% if (mode === 'edit') { %>
                            <small style="color: #666; display: block; margin-top: 0.25rem;">Leave blank to keep the current password</small>
                        <% } %>
                    </div>
                    
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <%= mode === 'create' ? 'Create User' : 'Update User' %>
                        </button>
                        <a href="/users" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </main>
    </div>
</body>
</html>

